


SCENARIO:

To update element you should to send special request
which can retrieve updated param data and and pass result to element 
for it's data update





TODO:

+ !!! The main issue is to retrieve values without passing params in validator
+ Nested update method
- Override schema.execute to get access to result
- Update during resolving
+ Replace el_id value 



"""
The response must be
{
    "update_element": {
        
        Schema definition !!!
        id
        name
        enable
        datetime
        dev {
        "ma_val": {
            "id": "ma_val",
            "type_id": "ShortString 550",
            "left": 550,
            "params": {
                "currency": {
                    "value": null
                },
                "s_in": {
                    "display_name": "New element"
                }
            }
        }}
    }
}



Validated types

- ShortString
- LongString
- Available params from list: currency, change, 
- DigitType - string representation of digit (type_id)
- PeriodSelect - periods 5, 15, 60, 240
- MA value -> positive value from 0 to 255

API:
 
    -> get schema         (Return json, filtering of deprecated params)
    -> save schema        (Save existing on memory schema)
    -> add param          (Copy skeleton and add to flowchart)
    -> update param       (Validation request is needed)
    -> delete param       (Delete from flowchart by id)


